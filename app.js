/* Pinglight — marketing site.
 *
 * The redesigned page (redesign/home) is a single reading column with one
 * static hero screenshot: no sticky rail, no multi-screen reel, no scroll-
 * driven animation. Nothing here needs a script to run, so this file is
 * intentionally empty. It stays in place — and stays linked from
 * index.html — so a future screen carousel can reuse the hook without a
 * markup change.
 */
